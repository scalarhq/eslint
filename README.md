# @modfy/eslint-config

## Usage

1. Install this package
    ```
    pnpm i @modfy/eslint-config
    ```

2. Extend in your `.eslintrc`
    ```json
    {
      "extends": "@modfy",
      "parserOptions": {
        "project": "./tsconfig.json"
      }
    }
    ```
